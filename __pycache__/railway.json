{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m uvicorn server:app --host 0.0.0.0 --port 8080",
    "healthcheckPath": "/api/chats/test",
    "healthcheckTimeout": 100
  }
}